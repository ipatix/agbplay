name: Build Common

on:
  push:
    tags:
      - 'v*'
    branches:
      - 'master'
  workflow_dispatch:
  pull_request:
    branches:
      - 'master'

jobs:
  build:
    name: Build Windows
    uses: ./.github/workflows/build-windows.yml
